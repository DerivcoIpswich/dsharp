"use strict";

define('AsyncAwait', ['ss'], function(ss) {
  var $global = this;
  // ExpressionTests.TestClass

  function TestClass() {
  }
  var TestClass$ = {
    test1: function() {
      (await this._doIt1());
    },
    test2: function() {
      var result = (await this._doIt1());
    },
    _doIt1: function() {
      return new promise(function(resolved, reject) {
        resolved(100);
      });
    }
  };


  var $exports = ss.module('AsyncAwait', null,
    {
      TestClass: ss.defineClass(TestClass, TestClass$, [], null)
    });


  return $exports;
});
